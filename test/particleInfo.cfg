process PROD  = {
   
       untracked PSet maxEvents = {untracked int32 input = 1} 
     
       source = EmptySource { } 
      
       include "SimGeneral/HepPDTESSource/data/pythiapdt.cfi"
                                                                                                       
       module pdtanal = HepPDTAnalyzer {

# print properties for a specified particle name
             #string particleName = "mu+"

# print properties for all the particles in the table use
             string particleName = "all"

# print only the names for all the particles used
             #string particleName = "names"
       }
                                                                                                       
       path p = { pdtanal }
                                                                                                       
}
