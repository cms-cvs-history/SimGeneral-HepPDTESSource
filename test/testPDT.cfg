process TEST  = {
  source = EmptySource { 
    untracked int32 maxEvents = 5 
  }
  module print = AsciiOutputModule { 
  }
  module get = EventSetupRecordDataGetter { 
    untracked bool verbose = true
    VPSet toGet = {
      { 
        string record = "PDTRecord"
        vstring data = {
          "DefaultConfig::ParticleDataTable"
        } 
      } 
    }
  }

include "SimGeneral/HepPDTESSource/data/pdt.cfi"
  
  path p = { get }
  endpath ep = { print }
}
